<Window x:Class="WPF6_H.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF6_H"
        mc:Ignorable="d"
                Title="MainWindow" Height="250" Width="400"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <StackPanel Margin="15" VerticalAlignment="Top">
        <TextBox x:Name="InputTextBox" 
                 Height="50" 
                 Margin="0,0,0,10"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Text="Введите текст здесь..."/>

        <CheckBox x:Name="ShowDetailsCheckBox" 
                  Content="Показать детали"
                  Margin="0,0,0,10"
                  IsChecked="True"/>

        <Border Visibility="{Binding ElementName=ShowDetailsCheckBox, Path=IsChecked, Converter={StaticResource BoolToVisibilityConverter}}"
                Background="#FFF0F0F0"
                Padding="10"
                CornerRadius="5">
            <StackPanel>
                <TextBlock Text="{Binding ElementName=InputTextBox, Path=Text.Length, StringFormat='Длина текста: {0} символов', Mode=OneWay}"
                           Margin="0,0,0,5"
                           FontWeight="SemiBold"/>

                <ProgressBar Maximum="100"
                             Height="15"
                             Value="{Binding ElementName=InputTextBox, Path=Text.Length, Mode=OneWay}"
                             Margin="0,0,0,5"/>
            </StackPanel>
        </Border>
    </StackPanel>
</Window>