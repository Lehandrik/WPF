<Window x:Class="WPF7_HW.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF7_HW"
        mc:Ignorable="d"
                Title="MainWindow" Height="150" Width="350">
    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Grid Width="20" Height="20" Margin="0,0,5,0">
                                <Rectangle x:Name="rect" 
                                           RadiusX="3" 
                                           RadiusY="3" 
                                           Stroke="Black" 
                                           StrokeThickness="1"
                                           Fill="White"/>

                                <TextBlock x:Name="checkMark" 
                                           Text="X" 
                                           FontWeight="Bold" 
                                           FontSize="14"
                                           HorizontalAlignment="Center" 
                                           VerticalAlignment="Center"
                                           Opacity="0"/>

                                <Ellipse x:Name="indeterminateMark" 
                                         Width="8" 
                                         Height="8" 
                                         Fill="Black"
                                         HorizontalAlignment="Center" 
                                         VerticalAlignment="Center"
                                         Opacity="0"/>
                            </Grid>
                            <ContentPresenter Content="{TemplateBinding Content}" 
                                              VerticalAlignment="Center"/>
                        </StackPanel>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Opacity" Value="1"/>
                                <Setter TargetName="rect" Property="Fill" Value="LightGreen"/>
                            </Trigger>

                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="indeterminateMark" Property="Opacity" Value="1"/>
                                <Setter TargetName="rect" Property="Fill" Value="LightYellow"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="rect" Property="Stroke" Value="Blue"/>
                                <Setter TargetName="rect" Property="StrokeThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <CheckBox Content="Не отмечен" 
                  IsChecked="False"
                  Grid.Column="0"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"/>

        <CheckBox Content="Отмечен" 
                  IsChecked="True"
                  Grid.Column="1"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"/>

        <CheckBox Content="Неопределенное состояние" 
                  IsChecked="{x:Null}"
                  Grid.Column="2"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"/>
    </Grid>
</Window>
