<Window x:Class="WPF5_HW.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Панель настроек" Height="250" Width="300">

    <Window.Resources>
        <Style x:Key="InteractiveButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource HoverColor}"/>
                </Trigger>

                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="FontWeight" Value="Bold"/>
                </Trigger>

                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{StaticResource DisabledColor}"/>
                    <Setter Property="Opacity" Value="0.7"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <!-- Заголовок -->
        <TextBlock Text="Настройки приложения" Style="{StaticResource HeaderTextStyle}"/>

        <!-- Основные кнопки -->
        <Button Content="Сохранить" Style="{StaticResource InteractiveButtonStyle}"/>
        <Button Content="Отменить" Style="{StaticResource InteractiveButtonStyle}"/>

        <CheckBox x:Name="ToggleButton" Content="Включить режим" Margin="5" IsChecked="True"/>

        <Button Content="Специальная кнопка"
                Style="{StaticResource InteractiveButtonStyle}"
                IsEnabled="{Binding IsChecked, ElementName=ToggleButton}"/>
    </StackPanel>
</Window>
